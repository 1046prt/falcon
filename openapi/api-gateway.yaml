openapi: 3.0.3
info:
  title: Falcon API Gateway
  version: '0.1.0'
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
  /upload:
    post:
      summary: Upload entrypoint (proxy to upload-service)
      responses:
        '200':
          description: Accepted
